/**
 * PLACEHOLDER — replaced wholesale by `npm run gen -w @repo/types` once the
 * Laravel backend exposes its OpenAPI document at
 * http://api.flowerpot.test/docs/api.json (Scramble, not wired yet).
 */

export interface paths {
  [path: string]: unknown;
}

export interface components {
  schemas: Record<string, unknown>;
}

export type operations = Record<string, never>;
export type webhooks = Record<string, never>;
