/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const ATTENDANCE_ENDPOINT = "/attendances";

/** Tamaño de página del listado de asistencias. */
export const ATTENDANCE_PER_PAGE = 20;
