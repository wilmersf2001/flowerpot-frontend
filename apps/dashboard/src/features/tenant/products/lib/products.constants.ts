/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const PRODUCTS_ENDPOINT = "/products";

/** Tamaño de página por defecto del listado de productos (igual que el backend). */
export const PRODUCTS_PER_PAGE = 15;
