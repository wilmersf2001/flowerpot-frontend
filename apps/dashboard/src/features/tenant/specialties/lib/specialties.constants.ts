/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const SPECIALTIES_ENDPOINT = "/specialties";

/**
 * Tamaño de página del listado de especialidades. Es un catálogo acotado, así
 * que se trae todo en una sola página en vez de paginar.
 */
export const SPECIALTIES_PER_PAGE = 100;
