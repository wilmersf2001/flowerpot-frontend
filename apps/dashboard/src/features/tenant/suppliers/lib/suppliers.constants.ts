/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const SUPPLIERS_ENDPOINT = "/suppliers";

/**
 * Tamaño de página del listado de proveedores. Es un catálogo acotado, así
 * que se trae todo en una sola página en vez de paginar.
 */
export const SUPPLIERS_PER_PAGE = 100;
