/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const BRANCHES_ENDPOINT = "/branches";

/**
 * Tamaño de página del list de sedes. Las sedes de un gimnasio son una lista
 * acotada, así que se trae todo en una sola página en vez de paginar.
 */
export const BRANCHES_PER_PAGE = 100;
