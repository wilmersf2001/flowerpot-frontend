/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const JOB_POSITIONS_ENDPOINT = "/job-positions";

/**
 * Tamaño de página del list de cargos. Los cargos de un gimnasio son una
 * lista acotada, así que se trae todo en una sola página en vez de paginar.
 */
export const JOB_POSITIONS_PER_PAGE = 100;
