/** Ruta del recurso en la API (el `apiClient` le antepone `/api/proxy`). */
export const EQUIPMENT_CATEGORIES_ENDPOINT = "/equipment-categories";

/**
 * Tamaño de página del listado de categorías. Es un catálogo acotado, así
 * que se trae todo en una sola página en vez de paginar.
 */
export const EQUIPMENT_CATEGORIES_PER_PAGE = 100;
