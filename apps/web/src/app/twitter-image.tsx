/**
 * X/Twitter usa la misma imagen que Open Graph. Reexportamos el generador de
 * `opengraph-image` para no duplicar el diseño.
 */
export { alt, size, contentType, default } from "./opengraph-image";
